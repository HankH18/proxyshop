# ESCALATIONS — proxyshop

The loop writes here when it hits something frozen (or otherwise locked) that it
believes is **wrong**. A freeze is a locked door, not a wall: the loop's obligation is
to bring the problem to you rather than work around it, silently re-freeze it, or keep
scoring against a goal it thinks is broken.

**Nothing in this file has been changed by the loop. Escalating is asking. It is never
granting.** Every entry is a request awaiting your decision. Only you can grant one:
`freeze --amend "<reason>"` is the only mechanism that alters a frozen goal or test,
and `escalate --resolve <ID>` records what you decided.

<sub>Mirror of `.swarm-loop/escalations.jsonl` (append-only — that file is the source of truth). Regenerated by
every `escalate`; editing this file by hand changes nothing.</sub>

## OPEN (1)

### ESC-020 · frozen-test · `.swarm-loop/acceptance emits self-declared verified claims — and that is now what lets a bidder fake a hard constraint on a SERVED path`

- **filed**: 2026-09-04T17:07:25 · cycle 18 · HEAD `653b313374`
- **what is wrong**: WIRING THE RANKER MADE A PRE-EXISTING TRUST BYPASS REACHABLE BY AN HTTP REQUEST, AND THE HONEST FIX TAKES FROZEN TESTS RED. This is the ESC-017 shape again: satisfying the product requirement breaks a frozen test, satisfying the frozen test keeps the fail-open.

THE DEFECT, confirmed at source on the merged tree. apps/exchange/src/ranking/filters.py:237 gates hard-constraint satisfaction on: if str(_enum_value(read(claim, 'status', ''))) != VERIFIED. That status field is supplied by the bidding store in its OWN claim; the published Claim contract does not declare it (additionalProperties: false); and nothing on the auction path validates claims against that contract. Measured by the lane: two otherwise identical stores, one adding the string, and the liar takes the ENTIRE shortlist while the honest store is excluded. It also sets verified_hard_fit_count, which is the FIRST published tie-break, so the lie wins ties as well as filters.

WHY IT IS NEWLY URGENT. rank() was on no served path until this cycle — T-310's whole finding was that the ranker was orphaned while the demo claimed it ran. Wiring it onto POST /auctions is correct and closes T-310, and it also means an unauthenticated request now reaches this branch. The defect did not change; its REACHABILITY did, and my merge is what changed it.

WHY I AM NOT JUST FIXING IT. The obvious repair — ignore store-supplied status, treat every self-asserted claim as unverified — makes every hard-constrained auction return an EMPTY shortlist, because nothing in this system currently produces a verified claim on the auction path. And the fixtures that go red are not only the lane's: apps/exchange/tests/_fixtures_ranking.py and test_ranking.py emit self-declared verified claims, AND SO DOES THE FROZEN ACCEPTANCE SUITE. The honest fix takes frozen tests red, which I may not do.

I HAVE NOT WORKED AROUND IT AND HAVE NOT SOFTENED THE TICKET. It is minted at HIGH with its reproduction; the merge landed with make verify green, the frozen suite at 120/120, the artifact gate at 10 and collect-only 5611 with zero errors; and this is filed the same cycle rather than carried.

NOT BLOCKING, deliberately. Nothing is deployed, no other work depends on this ruling, and there is plenty of honest work that does not touch it. I would rather keep building than halt a swarm over a decision you can take when you reach it.
- **proposed change**: Rule one of three. (1) MY RECOMMENDATION — amend the frozen acceptance FIXTURES so claims carry verification provenance the store cannot write, then fix filters.py to ignore store-supplied status. It is the only option that actually closes the bypass, and it touches the suite's fixtures rather than its assertions. (2) Wire a real claim-verification producer on the auction path first and let the bypass close as a consequence — better engineering, substantially more work, and the bypass stays live meanwhile. (3) Rule that self-asserted claims are in scope for this build's demo and not a defect; I will record that on the ticket and stop treating it as one. That is coherent for something undeployed, and I want it written down rather than assumed.
- **to grant it** — edit .swarm-loop/acceptance emits self-declared verified claims — and that is now what lets a bidder fake a hard constraint on a SERVED path, then re-lock the harness around the new bytes:

  ```
  swarmloop.py freeze --amend "<why you approved this harness change>"
  swarmloop.py escalate --resolve ESC-020 --note "<what you decided>"
  ```
- **to decline it**:

  ```
  swarmloop.py escalate --resolve ESC-020 --note "declined — <why>"
  ```

## RESOLVED (19)

### ESC-019 · frozen-test · `.swarm-loop/acceptance/test_e8_proofs.py — the runbook gate still checks SHAPE, and step (3) of your own ESC-018 ruling has now arrived` — resolved 2026-09-04T16:46:20

- filed 2026-09-04T16:42:50 (cycle 18)
- **what was wrong**: YOUR RULING ON ESC-018 SAID A FRESH ESCALATION MUST BE FILED AT STEP 3. This is it, and the conditions you set have been met.

You ruled: (1) land the executability check in the NON-FROZEN tier — done, it is on main; (2) make the runbook's claims TRUE — done, this cycle; (3) only then revisit amending the frozen gate, 'since a shape check will certify the next broken runbook too, but doing it after the runbook is executable costs nothing where doing it first deliberately reds a confirmed-green board.'

STEP 2 IS COMPLETE AND MEASURED. The non-frozen executability gate reports 17 commands / 17 PASS / 0 FAIL, down from 13 PASS / 4 FAIL, with its xfail marker removed. e2e/test_s1_flow.py exists and passes 27 tests in 1.2-1.5s. make e2e-live resolves, with all four of its exit statements audited and none of them zero. make verify is OK:all on the combined tree. The lane reproduced T-082's original rejection first — on the old draft AND on current main, replacing the HMAC verify with a constant and mint_code with a constant are STILL not caught, with probes proving the patched functions ran inside the real flow — and its own suite catches 9 of 9 sabotages.

SO THE COST YOU WERE AVOIDING IS NOW ZERO. Amending the frozen E8 runbook test to grade executability rather than shape would leave it GREEN today, because the runbook's commands now actually resolve. There is no confirmed-green board to red. e8_proofs_passing stays 8, acceptance stays 120/120.

WHAT IS STILL WRONG WITHOUT THE AMENDMENT, which is why the question did not go away when the runbook was fixed: the frozen test checks that the runbook's make targets are DEFINED. It never checks that their recipes resolve. So it certified a runbook whose two headline commands both failed, for an unknown number of cycles, at 8/8 — and it will certify the next one identically. The non-frozen gate now covers this, but the non-frozen tier is worker-editable by design: check-branch vetoes a lane touching .swarm-loop/acceptance and vetoes nothing about docs/tests/.

THE COUNTER-ARGUMENT, stated because it is real. A gate that is green when it lands can only ever go red later, and this one grades a document a lane may legitimately edit. The frozen suite currently has zero tests that execute a shell recipe, so this would be the first, and freeze-time hermeticity rules apply to it (it must pin its own configuration and not read anything outside the frozen manifest). If you would rather the executability check stay in the non-frozen tier permanently and accept that a worker could weaken it, that is a coherent answer and I will record it and stop asking.
- **proposed**: Amend .swarm-loop/acceptance/test_e8_proofs.py's runbook test so it grades EXECUTABILITY rather than shape: for every make target the runbook names, assert the target is defined AND every script/file its recipe invokes exists (and parses, for shell); for every pytest/python path named, assert the path exists and pytest can COLLECT it. Derive the command list from the runbook text rather than hardcoding it — a gate over a frozen list of command names stops working the moment the runbook changes, which is the failure the current test already has. It goes green on today's tree, so no metric moves. If you prefer, the narrower version is to assert only the two headline commands resolve, which is smaller but re-acquires the same staleness problem the first time the runbook is edited.
- **decision**: GRANTED by Hank, 2026-09-04, in session: 'Cool, yeah, then do it.' — said directly in reply to the message that ended with ESC-019 and its resolve command, so I am reading it as authorising the ESC-019 amendment specifically. RECORDING MY READING EXPLICITLY BECAUSE A PEER MESSAGE ABOUT A DIFFERENT AMENDMENT ARRIVED IN THE SAME WINDOW: this note authorises amending .swarm-loop/acceptance/test_e8_proofs.py's runbook test to grade EXECUTABILITY rather than shape. It does NOT authorise anything else, and in particular it is NOT the authority under which the separate 130-ticket verify-field amendment proceeds — that one runs on the pre-existing standing grant for the tickets.json verify-field class (ESC-013, 'GRANTED by Hank 2026-09-03'), which Hank declined to intervene against when a peer session raised it but did not re-confirm. If my reading of 'do it' is wrong, this is the record that shows exactly what I took it to mean. CONDITIONS THAT MADE THE COST ZERO, all measured before asking: the runbook's claims are now true (17 commands, 17 PASS, 0 FAIL, xfail marker removed), e2e/test_s1_flow.py passes 27 tests, make e2e-live resolves with no reachable exit 0, and make verify is OK:all — so the amended frozen test goes green on today's tree, e8_proofs_passing stays 8 and acceptance stays 120/120. No board is taken red, which is exactly the sequencing Hank ruled on in ESC-018.

### ESC-018 · frozen-test · `acceptance/test_e8_proofs.py:677 certifies a demo runbook whose two headline commands both fail` — resolved 2026-09-04T12:48:17

- filed 2026-09-04T12:17:17 (cycle 18)
- **what was wrong**: THE FROZEN E8 GATE CERTIFIES A DEMO RUNBOOK WHOSE TWO HEADLINE COMMANDS BOTH FAIL. e8_proofs reads 8/8 and acceptance reads 120/120 while docs/demo/starting-slice.md's entire sections 3 and 4 -- the part an operator would run in front of an audience -- have no executable behind them.

MEASURED: (1) starting-slice.md:189 instructs 'uv run python -m pytest e2e/test_s1_flow.py -q', its headline proof. That file is NOT on main -- pytest exits 4, 'file or directory not found'. It exists only on the unmerged T-082 branches; T-082 was REJECTED this session on its merits (two sabotage-proven blind spots: replacing the HMAC verify with lambda:True, or mint_code with a constant, each left all 23 of its tests passing). (2) starting-slice.md:228 instructs 'make e2e-live'; docs/demo/e2e_live.sh DOES NOT EXIST, exit 127. The Makefile target is real and the script was never written.

WHY THE GATE MISSES BOTH: .swarm-loop/acceptance/test_e8_proofs.py:677-692 checks that the make target is DEFINED IN THE MAKEFILE. It never checks that the target's script exists, and it never checks that any command the runbook instructs actually resolves. So the gate grades the runbook's SHAPE -- headings, a t-053 reference, a live-LLM mention, the make targets it names -- and not whether following it does anything. That is the same class as the Dockerfile findings T-298..T-301: a gate that grades the artifact's shape rather than its behaviour, passing while the thing it certifies is inert.

THE INFORMATION WAS ALREADY IN THE REPO AND NO GATE CONSUMED IT. 'make check' prints '7 ticket verify path(s) not created yet:' and lists 'T-082 e2e/test_s1_flow.py' by name. So the repo knows the file is missing, says so on every run, and the frozen suite certifies the runbook that depends on it regardless.

NOT A CRITICISM OF THE T-082 REJECTION, which I stand behind: a rebuilt T-082 with a gate that can go red is worth more than a merged one that cannot. The defect is that rejecting a ticket silently invalidated a runbook certified by a frozen test, and nothing anywhere noticed.
- **proposed**: Amend .swarm-loop/acceptance/test_e8_proofs.py's runbook test so it grades EXECUTABILITY, not shape. Two concrete additions, both cheap and both static -- no subprocess, no network, no daemon:

 1. For every 'make <target>' the runbook instructs, resolve that target in the Makefile and assert every script or file its recipe names EXISTS. That alone catches make e2e-live -> docs/demo/e2e_live.sh.
 2. For every path a runbook command names as a pytest argument or a script path, assert the path exists in the tree. That catches e2e/test_s1_flow.py.

I am NOT proposing the test execute the runbook -- that would need Docker, would be slow, and would make the frozen suite non-hermetic, which the harness rules out for good reason. Existence is the right depth: it is the difference between 'this document is well-formed' and 'this document's instructions resolve'.

RECOMMENDED SEQUENCING, and I want your ruling on it rather than assuming: this amendment would take e8_proofs from 8/8 to 7/8 and acceptance from 120/120 to 119/120 THE MOMENT IT LANDS, and it stays red until either T-082 is rebuilt and merged or the runbook is edited to stop naming what does not exist. That is the correct behaviour -- the metric would finally be telling the truth -- but it means deliberately taking a green board red, at a point where 120/120 has been measured, pushed and independently confirmed by a second session.

Three options: (a) LAND THE GATE FIRST and accept a red board until the runbook's claims are made true, which is the honest ordering and my recommendation; (b) fix the runbook first -- write e2e_live.sh and rebuild T-082 -- then land the gate, so the board never goes red but the gap stays uncaught for however long that takes; (c) land the gate and simultaneously edit the runbook to remove the two unrunnable commands, which keeps the board green and is the option I like least, because it resolves a documentation-versus-reality conflict by deleting the documentation's ambition.

This is a frozen path so it is yours either way, and unlike ESC-017 there is no urgency: the defect is that a green number overstates what works, and the run is already stopped.
- **decision**: USER DECISION (Hank, 2026-09-04, in session): do NOT amend the frozen E8 test now. Sequence it the other way instead — (1) land the executability check in the NON-FROZEN tier, which needs no amendment and takes no metric red (lane-C19-runbook, in flight); (2) make the runbook's claims TRUE — write docs/demo/e2e_live.sh and land e2e/test_s1_flow.py — which dissolves the failure at its source; (3) only then revisit amending the frozen gate, since a shape check will certify the next broken runbook too, but doing it after the runbook is executable costs nothing where doing it first deliberately reds a confirmed-green board. A FRESH escalation must be filed at step 3 — this resolution defers that question, it does not answer it.

### ESC-017 · frozen-test · `acceptance/test_e4_store_agent.py:1180 asserts the permissive side of T-233's fail-open` — resolved 2026-09-04T03:01:04

- filed 2026-09-04T02:47:53 (cycle 17)
- **what was wrong**: T-233 CANNOT BE CLOSED HONESTLY BY ANY LANE, because the frozen acceptance suite MANDATES the defect. This is not a ticket-vs-test conflict: the frozen suite EXHIBITS the T-233 asymmetry on its OWN payload and store id, and ASSERTS THE PERMISSIVE SIDE.

MEASURED by an adversarial verifier against the UNMODIFIED door, using the frozen suite's own payload helper:
    eligibility OMITTED  -> accepted=True   reasons=()
    trust_snapshot={}    -> accepted=False  reasons=('trust_snapshot_unavailable:store-external-1')
That is exactly T-233's complaint -- the ABSENT argument is more permissive than an explicit one. And .swarm-loop/acceptance/test_e4_store_agent.py:1180 asserts accepted is True on precisely that call. The requirement sets are LOGICALLY INCONSISTENT: satisfying T-233 breaks a frozen test, and satisfying the frozen test keeps T-233 open forever.

FOUR ESCAPE HATCHES WERE IMPLEMENTED AND MEASURED, ALL DEAD:
 (1) a sentinel distinguishing omitted-from-explicit-None fails because NOT ONE of the 14 accepting calls passes blacklist=, and the only two that do demand rejection;
 (2) a real eligibility source is dead -- both sides use store_id store-external-1;
 (3) making an explicit empty dict accept is forbidden by T-233's own control at test_repro_external_door.py:522;
 (4) fixing in contracts.boundary is dead because the submission is identical both ways.
T-232's remedy is also unavailable: the frozen file passes nonce_store= on all 20 calls but eligibility on exactly 1, and that 1 demands rejection.

ALL 14 RELEVANT TESTS ARE POSITIVE CONTROLS -- each needs one valid bid admitted before it can test signatures, nonces, freshness or blacklists. None is a test ABOUT eligibility, which is why the permissive assertion is incidental rather than intended.

THE DANGER, and why this must be recorded BEFORE any lane picks T-233 up: a FAKE GREEN is reachable and a future agent will find it. Keying _synthetic_trust_snapshot on claims -- if not payload.get("claims"): return {} -- turns ALL THREE suites green (7/30/20) while the defect survives. It is backwards twice over: claims=[] (the verifier's probe) becomes not-more-permissive, while claims=[1] (any realistic bid) stays more permissive. It was implemented, measured, and deliberately NOT left in place.

ROOT CAUSE: T-233's test is a SINGLE-PAYLOAD probe of a UNIVERSAL property, so it is gameable by construction.
- **proposed**: Amend .swarm-loop/acceptance/test_e4_store_agent.py so its positive controls pass an eligibility argument the way they ALREADY pass nonce_store= on all 20 calls. That is the minimal change: those 14 tests are about signatures, nonces, freshness and blacklists, none is about eligibility, and the permissive assertion at :1180 is incidental to what each test exists to prove -- so supplying eligibility explicitly weakens none of them.

This is a frozen path, so only you can grant it. I am NOT asking to weaken a frozen assertion: the change makes those controls state a precondition they currently leave implicit.

Two things land alongside it once you rule, and they are mine to do:
 - T-233's reproduction must be PARAMETRIZED over payloads rather than probing a single one. A single-payload probe of a universal property is gameable by construction, and that is precisely what makes the fake green above reachable.
 - T-281 must be re-scoped. It currently names only test_repro_external_door.py:283, and fixing that alone will NOT unblock T-233 while the frozen file pins the same fail-open.

If you would rather not touch the frozen suite at all, the honest alternative is to mark T-233 permanently blocked and say so in the final report -- but then the fail-open ships, and the run's own acceptance suite is the reason it shipped.
- **decision**: GRANTED IN PRINCIPLE by the orchestrator, on Hank's explicit delegation, 2026-09-04 cycle 17 -- with execution gated on independent confirmation of the premise. Recording the reasoning in full, because this authorises touching the frozen acceptance suite and "the orchestrator decided" is not an audit trail.

THE RULING. A frozen acceptance suite that MANDATES a security fail-open is the worst thing a frozen suite can do, and it is strictly worse than the suite being merely wrong: it converts the anti-gaming mechanism into the thing protecting the defect. T-233 is a fail-open where the ABSENT eligibility argument is more permissive than an explicit one, and .swarm-loop/acceptance/test_e4_store_agent.py:1180 asserts accepted is True on exactly the call that exhibits it. Satisfying T-233 breaks a frozen test; satisfying the frozen test keeps the fail-open forever. Four escape routes were implemented and measured dead. So the choice is between amending the suite and shipping a known fail-open whose own justification is the run's acceptance criteria. I am granting the amendment.

WHY THE AMENDMENT DOES NOT WEAKEN ANYTHING. All 14 relevant tests are POSITIVE CONTROLS -- each needs one valid bid admitted before it can test signatures, nonces, freshness or blacklists. NONE is a test about eligibility. The permissive assertion is incidental to what each test exists to prove, and the same file already passes nonce_store= explicitly on all 20 calls. Supplying eligibility the same way makes a precondition explicit that is currently implicit. It is not a relaxation; it is the controls saying what they mean.

WHY EXECUTION IS GATED RATHER THAN IMMEDIATE. The premise -- that the frozen suite asserts the permissive side -- comes from ONE adversarial verifier. It is thorough and it measured rather than argued, and I still will not move a frozen byte on a single source at the exact moment that suite reads 120/120. That is precisely when tampering is most tempting and least visible. So: an independent reader confirms the premise against the frozen bytes FIRST, and only then does the amendment land.

CONDITIONS ON THE AMENDMENT, all of which must hold and be shown:
 1. The collected test COUNT stays 120. A frozen suite whose size changes is a different suite.
 2. No existing assertion's meaning changes -- the only edit is supplying an argument the controls already leave implicit.
 3. It is demonstrated in both directions: T-233's fix becomes possible, and every one of the 14 controls still fails for its own reason when its own subject is broken.
 4. T-233's reproduction is PARAMETRIZED over payloads in the same change. A single-payload probe of a universal property is gameable by construction, and that is exactly what makes the known fake green reachable -- keying the synthetic snapshot on claims turns all three suites green while the defect survives for every realistic bid. Amending the suite without fixing the probe would hand the next lane a wider hole.
 5. T-281 is re-scoped, since it names only test_repro_external_door.py:283 and fixing that alone cannot unblock T-233 while the frozen file pins the same fail-open.

COST STATED PLAINLY, and it goes in the final report: the 120/120 measured at 7b5d1f0 and pushed at c3fc49a was achieved against the PRE-amendment suite. After this lands, that number is against a suite that has since changed, and any comparison across the boundary must say so.

### ESC-015 · harness · `T-082's verify gate, and the deliverable-is-its-own-gate class (T-263)` — resolved 2026-09-04T03:00:15

- filed 2026-09-04T01:55:03 (cycle 16)
- **what was wrong**: T-082 CANNOT MERGE THROUGH THE SANCTIONED PATH, and no amount of re-running clears it. Its gate is 'PROXYSHOP_WORKER=0 uv run python -m pytest e2e/test_s1_flow.py -q' and e2e/test_s1_flow.py is T-082's OWN DELIVERABLE. red-check --close evaluates at the MERGE BASE; measured: git cat-file -e 9ef97f8:e2e/test_s1_flow.py is ABSENT, at the branch tip 3686298 it is PRESENT. The command against a tree lacking the file gives 'no tests ran', non-zero exit, ZERO selected -- which is WEAK by definition. Two stamps now exist, both WEAK (pre-dispatch 00:33:05, retro close 01:50:52), and a third would be identical however good the work is. check-branch --ticket T-082 reads that stamp as a BLOCKING veto because the ticket is not ledger-predating. So the veto can never be legitimately cleared for this class. I ALSO MEASURED THE OBVIOUS WORKAROUNDS AND REJECT THEM: (a) nothing in .swarm-loop/acceptance grades the S1 flow at all -- grep for s1_flow/S1 returns nothing -- so I cannot repoint the gate at a frozen test that genuinely grades this work, because none exists; (b) the bootstrap:true exemption is currently declared on ZERO tickets so the slot is free, but its contract reserves it for the graph's sole root whose declared outcome IS a green pipeline on an empty suite, and the skill names 'widening the exemption to get a wave moving' as precisely the wrong reason -- I will not take it; (c) amending the gate to name some other already-passing test would manufacture a green and is the reward-hacking shape the whole ladder exists to stop. WHAT IS AT STAKE: this branch carries T-085 as well as T-082, and T-085's runbook work is what takes e8_proofs from 7/8 to 8/8. An independent session measured integration/17-3 at 120/120 with every epic at target and the frozen manifest verified unchanged (17/17 hashes, amendments still 16, no target drift). So holding this branch holds the run's last frozen metric short of target. I am NOT treating that as a reason to lower the bar -- it is the reason to put the decision in front of you rather than make it myself.
- **proposed**: Rule one of three. (1) HARNESS FIX, my recommendation: give red-check a mode for tickets whose deliverable IS their gate target -- evaluate the gate at the branch TIP and require that it FAILS when the ticket's own new files are removed, which is a real red that this class can actually produce. That is a swarm-loop change, not a proxyshop one, and it is filed as T-263 plus a harness entry. (2) NARROW RULING: authorize merging task/T-082 on recorded alternative evidence -- T-085's gate is legitimately red at base and now green, the frozen suite reads 120/120, and the branch's deletion audit and suite numbers are recorded -- with T-082 itself explicitly marked as landed WITHOUT a red-at-base gate in the cycle report, so the gap is on the record rather than papered over. (3) Widen the bootstrap:true exemption to cover deliverable-is-its-own-gate tickets, which I do not recommend, because it is a general loosening bought to move one branch.
- **decision**: DECIDED by the orchestrator, on Hank's explicit delegation ("the three things you were waiting on me for, I'll take your best judgment on"), 2026-09-04 cycle 17. Ruled: option 1, the harness fix, and NOT options 2 or 3.

The instance is already resolved without needing any of the three: T-082 was rejected back to the backlog on its own merits -- two sabotage-proven blind spots (replacing the HMAC verify with lambda:True, or mint_code with a constant, each leaves all 23 of its tests passing, confirmed by a controlled matched pair), three ruff I001 errors that would take build_succeeds from 1 to 0, and it was never gated by its own lane before that lane died. Its branch and worktree are preserved as the retry packet's input.

Option 2 (merge on recorded alternative evidence) is therefore moot and would have been wrong anyway: the work is not good enough to merge on any evidence. Option 3 (widen bootstrap:true) is REFUSED on principle -- that field's contract reserves it for the graph's sole root whose declared outcome genuinely IS a green pipeline on an empty suite, and taking it here would be widening an anti-gaming exemption to move one branch, which is the exact reason the skill names for refusing it.

The CLASS is real and outlives this instance: any ticket whose deliverable IS its gate target can never be red at its merge base, so red-check --close stamps WEAK forever and check-branch vetoes a branch that did nothing wrong. Recorded as T-263 in this project's graph and filed machine-wide for the skill owner. The fix belongs in swarm-loop, not proxyshop: evaluate such a gate at the branch TIP and require it to FAIL when the ticket's own new files are removed -- a red this class can actually produce. Nothing in this repo is blocked on it; the next lane to rebuild T-082 will need it, and until it exists that ticket cannot be closed through the sanctioned path.

### ESC-016 · harness · `tickets.json` — resolved 2026-09-04T03:00:15

- filed 2026-09-04T02:23:04 (cycle 17)
- **what was wrong**: T-250's port invalidated two filed reproductions in the frozen ticket graph, and anyone re-running them will MIS-MEASURE rather than fail. (1) tickets.json:4716 records T-271's reproduction as 'set MINIMUM_PAYABLE_AMOUNT=0.0 in apps/exchange/src/auction/collect.py, run pytest apps/exchange -> 695 passed'. collect.py no longer DEFINES that constant: :163-164 import MINIMUM_PAYABLE_AMOUNT and PRICE_FLOOR_FRACTION from contracts.boundary and :221-222 merely re-bind them, with the comment at :212-220 saying so ('re-exported from the shared boundary rather than defined here (T-250) ... only the definitions moved'). The arithmetic is now packages/contracts/src/boundary.py:229-250, and collect.py:457 calls contracts.boundary.price_floor(listed), which reads the boundary's own module globals -- so the filed mutation is a guaranteed no-op and its green result reads as 'the floor is unpinned' when the mutation never reached the floor. (2) Same cause, second instance: tickets.json:4788 records T-276's location as 'apps/exchange/src/auction/collect.py:437' and its defect text names the symbol _price_floor. No symbol _price_floor exists in collect.py any more (only _below_the_price_floor at :418, which since T-250 reports nothing and only answers _is_judged), and collect.py:437 is now the middle of a docstring paragraph about T-250 itself. Verified: grep -n 'MINIMUM_PAYABLE_AMOUNT' apps/exchange/src/auction/collect.py -> import at :163, re-bind at :221, no definition; grep -n '_price_floor' apps/exchange/src/auction/collect.py -> no such symbol. Recorded as a cycle-17 finding; the findings tool refused it a ticket because tickets.json is a protected path no worker may touch.
- **proposed**: Amend the frozen graph so both records point at the surviving site. T-271 (tickets.json:4713,4716): change location to packages/contracts/src/boundary.py:212 and rewrite the reproduction to mutate contracts.boundary.MINIMUM_PAYABLE_AMOUNT / PRICE_FLOOR_FRACTION (packages/contracts/src/boundary.py:212 and :226) rather than the re-exported names in collect.py, re-measuring the pass count on the current tree rather than carrying '695 passed' forward. T-276 (tickets.json:4788): change location to packages/contracts/src/boundary.py:229-250 and replace '_price_floor' with 'contracts.boundary.price_floor' in the defect text; T-276's underlying dead-band behaviour is unchanged and still open, and its blast radius is now wider than filed -- the same arithmetic is mirrored in the TypeScript peer at packages/contracts/src/ts/boundary.ts:206-210. No product code changes; this is a re-measurement recorded through 'freeze --amend', never a hand edit of tickets.json.
- **decision**: GRANTED by the orchestrator, on Hank's explicit delegation, 2026-09-04 cycle 17. Both stale reproductions will be repointed at the surviving site.

The defect is that T-250's port moved the price floor from apps/exchange/src/auction/collect.py into packages/contracts/src/boundary.py, and two records in the frozen graph still describe the old location. They do not merely go stale -- they MIS-MEASURE. T-271's recorded reproduction says "set MINIMUM_PAYABLE_AMOUNT=0.0 in collect.py -> 695 passed"; after the port that mutation is a guaranteed no-op, so anyone re-running it concludes the floor is unpinned when it is merely no longer there. T-276's location names the symbol _price_floor in collect.py, and no such symbol exists there any more.

Granted because this is a strictly-corrective change to LOCATION and REPRODUCTION fields: it moves no target, changes no metric, weakens no assertion, and closes no ticket. It makes two records describe the code that actually exists. Refusing it would leave two booby-trapped reproductions in the graph for a future lane to trust.

Applied as a freeze --amend restricted to those fields, with the diff verified to touch nothing else.

### ESC-013 · harness · `tickets.json verify fields — amendment 10, MEASURED. Supersedes ESC-011 and ESC-012, which I am withdrawing` — resolved 2026-09-03T20:54:51

- filed 2026-09-03T20:17:07 (cycle 15)
- **what was wrong**: I FILED ESC-011 ON A DIAGNOSIS I HAD NOT MEASURED, AND IT WAS WRONG — this corrects it before you act on it, which is the same mistake amendment 8 made and amendment 9 caught. ESC-011 said the nine epic gates carry ONE defect (bare 'pytest' resolving to Anaconda 6.2.4 against pyproject.toml:70 minversion 8.2) and proposed prefixing them with 'uv run python -m'. That prefix alone does NOT fix them. A closure audit across all 72 READY tickets measured a SECOND defect I had not seen: five of these gates name a test FILE THAT DOES NOT EXIST — T-022 (services/ingest/tests/test_entity_resolution.py), T-023 (test_catalog_mcp.py), T-052 (apps/merchant/svc/tests/test_codes.py), T-053 (test_onboarding.py), T-071 (apps/buyer/svc/tests/test_intent.py). Fixing only the interpreter leaves them exiting 4 with 'file or directory not found', selecting nothing, still WEAK. This is exactly the half of ESC-010 that amendment 9's own text already identified — 'the verify names a file the lane must create' — and I proposed a repair that addressed the other half only. THERE IS A THIRD FINDING, AND IT IS THE WORST ONE: T-081's CURRENT gate is 'pytest services/sim -q', and it PASSES. It passes because the entire selection is one 50-line test_scaffold_smoke.py, while services/sim/src/__init__.py is ZERO BYTES and is the only source file in the package. T-081 is completely unbuilt and its recorded gate certifies it as done — the same class T-160 already documents for T-109/T-111/T-123, where a verify passed identically with and without the defect and three tickets were reported closed twice with their defects live. A gate that is GREEN on unbuilt work is more dangerous than one that is WEAK, because nothing ever looks at it again. SCOPE CONTEXT YOU SHOULD HAVE: the same audit found that 49 of the 72 READY tickets carry the byte-identical placeholder 'false  # NO GATE YET' that swarmloop.py findings writes into every ticket it mints — not the seven I named in ESC-012. I am NOT asking you to rule on 49 gates. I have filed the underlying harness defect at ~/claude-build/observations/swarm-loop-HARNESS-OPEN.md as B09032015-01 (findings should accept a verify on the finding record and seal it in the mint it already re-seals, so an unattended audit loop can dispatch its own findings without waking you). This ask covers only the ten tickets the next two cycles actually need.
- **proposed**: Amendment 10, tickets.json verify fields ONLY, ten tickets, nothing else in the file. Point each epic gate at the FROZEN ACCEPTANCE TEST that already grades it — the same repair amendment 8 made — prefixed with 'uv run python -m' so it self-provisions. These tests EXIST, are committed, and are RED right now, so the gate is red at base for exactly the defect the ticket names. I MEASURED THREE OF THESE IN A HAND-BUILT BARE WORKTREE OF main (no .venv, no node_modules), rather than in the primary checkout, which is precisely where amendment 8 went wrong: T-023 gave '1 failed, 7 deselected in 0.52s' (3.2s wall including provisioning) on the real ImportError; T-081 gave '1 failed, 7 deselected' on the real ModuleNotFoundError; T-228 gave 'Found 8 errors in 3 files (checked 224 source files)'. EXACT STRINGS: T-022 -> uv run python -m pytest .swarm-loop/acceptance/test_e2_ingestion.py -q -k test_gtin_matches_always_link_and_below_threshold_pairs_do_not | T-023 -> uv run python -m pytest .swarm-loop/acceptance/test_e2_ingestion.py -q -k test_both_catalog_adapters_satisfy_one_interface | T-052 -> uv run python -m pytest .swarm-loop/acceptance/test_e5_merchant.py -q -k 'test_code_is_single_use_with_a_bounded_expiry or test_conflicting_combines_with_is_detected_before_redirect or test_duplicate_redemption_emits_an_integrity_event_not_an_exception' | T-053 -> uv run python -m pytest .swarm-loop/acceptance/test_e5_merchant.py -q -k 'test_interview_transcript_yields_the_expected_envelope_version or test_activation_requires_a_recorded_written_approval_artifact or test_envelope_edits_create_new_versions_and_old_versions_are_immutable' | T-071 -> uv run python -m pytest .swarm-loop/acceptance/test_e7_buyer.py -q -k 'test_clarification_loop_asks_at_most_three_questions or test_confirmed_intent_is_structured_with_use_case_constraints_and_budget_band or test_no_auction_is_created_before_the_buyer_confirms' | T-072 -> uv run python -m pytest .swarm-loop/acceptance/test_e7_buyer.py -q -k 'test_provenance_labels_map_store_confirmed_versus_from_their_website or test_accept_follows_the_permalink_returned_by_the_exchange' | T-073 -> uv run python -m pytest .swarm-loop/acceptance/test_e7_buyer.py -q -k 'test_feedback_prompt_is_offered_only_for_routed_orders or test_feedback_submission_lands_as_a_feedback_ledger_event' | T-081 -> uv run python -m pytest .swarm-loop/acceptance/test_e8_proofs.py -q -k test_dishonest_script_emits_exactly_the_manifest_behaviours | T-085 -> uv run python -m pytest .swarm-loop/acceptance/test_e8_proofs.py -q -k test_demo_runbook_has_the_required_sections_and_its_make_targets_exist | T-228 -> uv run python -m mypy. NO metric, target, acceptance test, product file, dependency or ticket dependency changes; the acceptance tests these now select are the SAME frozen tests that already grade these tickets in the epic metrics, so this cannot move a number. After you amend I will re-run red-check on all ten and report the stamps before merging anything. NOT BLOCKING: the five lanes in flight are building now and I am holding their merges, so nothing is idle waiting on this.
- **decision**: GRANTED by Hank 2026-09-03: 'Go with your best suggestions for both.' Landed as freeze --amend amendments 10 and 11 (freeze-log records 16 and 17), tickets.json verify fields only, git diff exactly 10 insertions / 10 deletions all on 'verify' lines. Amendment 11 was a self-correction of my own amendment 10 under the same approval: red-check stamped T-228 WEAK because its notion of a real gate is pytest-shaped and 'uv run python -m mypy' selects no tests, so T-228's gate now runs store-agent's suite before mypy. RESULT, measured retro (--close) against merge base 6b6040f in bare throwaway worktrees: T-023 red (1 selected), T-053 red (3 selected), T-081 red (1 selected), T-228 red (280 selected) — four real gates, and check-branch now returns OK on all four previously vetoed branches. RELEASED BY THIS: the merges of T-228/T-023/T-053/T-081, hence build_succeeds, hence the held push. Still gated on their own dispatch: T-022, T-052, T-072, T-073, T-085 have amended gates but are not built yet.

### ESC-014 · frozen-test · `.swarm-loop/acceptance/ — two frozen tests pass on implementations that do not do the thing (E2 seam test; three E5 envelope tests). REPORTING, not requesting an edit.` — resolved 2026-09-03T20:54:51

- filed 2026-09-03T20:45:17 (cycle 15)
- **what was wrong**: TWO INDEPENDENT LANES, EACH GRADED BY A FROZEN TEST IT HAD JUST SATISFIED, REPORTED THAT ITS OWN GREEN PROVES LESS THAN THE METRIC IMPLIES. I am carrying both to you because I may not touch a frozen test and because e2_ingestion_passing and e5_merchant_passing are about to be read as evidence these capabilities work. (1) test_both_catalog_adapters_satisfy_one_interface grades SHAPE ONLY — it never calls fetch_catalog or to_upserts. The lane that implemented it said so unprompted: 'a module exporting one *Adapter class with two stub bodies would satisfy it in full.' The behaviour behind that seam is graded only by services/ingest/tests/test_catalog_mcp.py, which the same lane wrote, so no frozen test vouches for it. This compounds with T-236, filed separately: NO production code constructs any CatalogAdapter at all — swarmloop.py reachable reports both the new CatalogMCPAdapter AND the already-merged SignedFetchAdapter and apply_upserts as UNREACHED from services/ingest/src/main.py, which mounts only extraction/routes.py while services/ingest/src/scheduler/ holds nothing but __init__.py. I asked the rung-2 discriminator and the answer is YES, a frozen acceptance test passes because of this code, so by the skill's own rule this is a measurement-credibility defect rather than merely unfinished wiring. (2) The three T-053 envelope tests never assert that a DIFFERENT envelope yields a DIFFERENT digest — test_e5_merchant.py:741-746 wraps the altered envelope's approval_digest in a try/except falling back to digest[::-1], so an approval_digest returning a CONSTANT STRING would satisfy every assertion in that test. They never assert an edit deactivates, and they never assert two versions do not share a mutable object; they assert only that _dump(v1) is unchanged, which an unmutated shallow copy also satisfies. The lane built to the stronger properties anyway and pinned each one in its own test_onboarding_envelope.py, so the approve-then-edit escalation IS closed — by the lane's tests, not by the frozen ones. WHY THIS MATTERS BEYOND THESE TWO: this is the run's standing hermeticity residual made concrete. The suite is necessary and nowhere near sufficient, and the same pattern already produced a live replay bypass in door.py that the frozen suite was green throughout — only the mypy errors made anyone look.
- **proposed**: NO ACTION REQUIRED FROM YOU RIGHT NOW, and I am deliberately NOT proposing an edit to the frozen suite. Frozen tests are frozen for a good reason and these two are not WRONG — they are NARROW, and narrow is what a black-box acceptance test written before the implementation usually is. What I am asking for is a RULING ON HOW TO READ THE NUMBERS, and it costs you nothing to defer: (a) accept that e2_ingestion_passing and e5_merchant_passing measure less than their names suggest, in which case I record exactly that beside them in every remaining cycle report and the final report, and the fix is product work already ticketed as T-236 (wire the ingestion pipeline) rather than a harness change; or (b) if you want the frozen suite strengthened to grade behaviour rather than shape, that is a freeze --amend on .swarm-loop/acceptance/ and it is a much larger decision than the gate respelling in ESC-013 — it changes what the metrics MEAN mid-run and would make cycles 0-15 not comparable with what follows, which is the exact goalpost problem the freeze exists to prevent. My recommendation is (a): leave the frozen suite alone, treat the narrowness as recorded fact, and close the gap with product tickets and lane-authored tests. NOT BLOCKING, and NOT gating any merge — ESC-013 is the one that gates merges. I will proceed under (a) unless you say otherwise.
- **decision**: DECIDED by Hank 2026-09-03 ('Go with your best suggestions for both') = my recommended option (a): LEAVE THE FROZEN SUITE ALONE. No freeze --amend on .swarm-loop/acceptance/; cycles 0-15 stay comparable with everything that follows. What I do instead, and will keep doing: record in every remaining cycle report and in the final report that e2_ingestion_passing and e5_merchant_passing measure LESS than their names suggest — the E2 seam test never calls fetch_catalog or to_upserts, so two stub bodies satisfy it in full, and the three E5 envelope tests would pass an approval_digest returning a constant string (test_e5_merchant.py:741-746 falls back to digest[::-1] in a try/except). The gap is closed by product work and lane-authored tests, not by moving a goalpost: T-236 wires the ingestion pipeline that no production code currently constructs, and the T-053 lane already pinned the stronger envelope properties in its own test_onboarding_envelope.py. This is the run's standing hermeticity residual made concrete rather than a new problem.

### ESC-011 · harness · `tickets.json — the 'verify' gate on T-022, T-023, T-052, T-053, T-071, T-072, T-073, T-081, T-085 and the placeholder on T-228 (amendment 10)` — WITHDRAWN by the loop 2026-09-03T20:17:07

- filed 2026-09-03T19:55:18 (cycle 15)
- **what was wrong**: SAME DEFECT AMENDMENT 9 ALREADY REPAIRED, ON TEN MORE TICKETS. Amendment 9 established that red-check builds a THROWAWAY worktree with NO .venv, so a gate spelled with a bare 'pytest' resolves to Anaconda's pytest 6.2.4 while pyproject.toml:70 sets minversion=8.2; pytest aborts with a usage error (exit 4) before selecting anything and the gate stamps WEAK, which check-branch --ticket then reads as a merge veto. I re-read all nine epic gates in tickets.json today and every one of them is spelled with the bare form amendment 9 fixed. TWO OF THEM CARRY A SECOND, INDEPENDENT FAULT: T-072 and T-073 put the 'npx vitest' half FIRST in an '&&' chain, so in a worktree with no node_modules the npx half fails and the pytest half — the only part that can select tests — never runs at all; the gate is WEAK no matter how the pytest half is spelled. EVIDENCE THAT THESE NINE TICKETS ARE ABOUT TO MATTER: I ran the frozen acceptance suite today and all 17 remaining failures across E2/E5/E7/E8 are ImportError or ModuleNotFoundError — nine more packages that were never written (services/ingest/src/er, services/ingest/src/adapters, apps/merchant/svc/src/codes, apps/merchant/svc/src/onboarding, apps/buyer/svc/src/intent, apps/buyer/svc/src/accept, apps/buyer/svc/src/feedback, services/sim/src/dishonest, docs/demo/). That is exactly the defect cycle 14 found in E3 and E4, and these nine tickets are the ones that close it. They cannot be merged through check-branch until their gates are real. SEPARATELY, T-228 was minted today by 'findings' from the 8 mypy errors holding build_succeeds at 0, and findings deliberately gives every minted ticket a placeholder verify that red-check refuses; that placeholder has to be replaced with a real gate before T-228 can dispatch, and T-228 is what unblocks the push of 40 commits. NOTHING ABOUT ANY METRIC, TARGET, ACCEPTANCE TEST, PRODUCT FILE OR DEPENDENCY CHANGES — this is tickets.json verify-fields only, exactly as amendments 8 and 9 were.
- **proposed**: Amendment 10, tickets.json verify fields ONLY, ten tickets. (1) Prefix the pytest command with 'uv run python -m ' — the same repair amendment 9 made — on T-022, T-023, T-052, T-053, T-081, T-085, and on the pytest half of T-071. (2) On T-072 and T-073, ALSO reorder the '&&' chain to put the pytest half first, so the half that selects tests runs before the npx half that cannot resolve in a bare worktree. (3) Replace T-228's findings placeholder with a real types gate: 'uv run python -m mypy'. Resulting strings — T-022: 'uv run python -m pytest services/ingest/tests/test_entity_resolution.py -q'; T-023: 'uv run python -m pytest services/ingest/tests/test_catalog_mcp.py -q'; T-052: 'uv run python -m pytest apps/merchant/svc/tests/test_codes.py -q'; T-053: 'uv run python -m pytest apps/merchant/svc/tests/test_onboarding.py -q'; T-071: 'uv run python -m pytest apps/buyer/svc/tests/test_intent.py -q && npx vitest run apps/buyer/app/intent'; T-072: 'uv run python -m pytest apps/buyer/svc/tests/test_accept_flow.py -q && npx vitest run apps/buyer/app/shortlist'; T-073: 'uv run python -m pytest apps/buyer/svc/tests/test_feedback.py -q && npx vitest run apps/buyer/app/feedback'; T-081: 'uv run python -m pytest services/sim -q'; T-085: 'uv run python -m pytest docs/tests/test_runbook.py -q'; T-228: 'uv run python -m mypy'. I will re-run red-check on all ten after the amend and report the stamps before dispatching anything through them. NOT BLOCKING: the lanes can build against these tickets while this is pending — the gate is a MERGE condition, not a build one — so I am dispatching and will hold the merges.
- **decision**: _NONE — NEVER ANSWERED. The loop withdrew the question_: Superseded by ESC-013, which I filed after measuring what ESC-011 only predicted. ESC-011's proposed repair (prefix the nine epic gates with 'uv run python -m') was INSUFFICIENT: a closure audit across all 72 READY tickets showed five of those gates also name a test file that does not exist, so the prefix alone leaves them selecting nothing, and T-081's gate is worse than weak — it PASSES on a zero-byte package. ESC-013 carries the corrected proposal, points the gates at the frozen acceptance tests instead, and reports three of them measured red in a bare worktree. Withdrawn because acting on ESC-011 as written would have produced a second amendment that did not work, not because the question went away.

### ESC-012 · harness · `tickets.json — the placeholder 'verify' on all seven finding-tickets T-221 through T-227, which includes the run's CRITICAL and HIGH defects (grant with ESC-011, one amendment)` — WITHDRAWN by the loop 2026-09-03T20:17:07

- filed 2026-09-03T20:12:57 (cycle 15)
- **what was wrong**: EVERY finding-ticket this run has ever minted is undispatchable, and nobody has noticed because the block is silent. All seven of T-221..T-227 carry the identical string that 'swarmloop.py findings' writes into a ticket it mints: verify = 'false  # NO GATE YET — write one that fails on this finding first'. That is deliberate on the harness's part — red-check refuses it, so a finding-ticket cannot dispatch until someone writes a gate that fails on the finding — and the consequence is that the run's two highest-severity known defects have been sitting in the graph unable to move. T-223 is CRITICAL: the price floor at packages/contracts/src/boundary.py:819 is an exact equality, so unit_price 0.001 buys a 100.00 product under max_discount_pct 100, and on an UNCAPPED roster row _is_judged (apps/exchange/src/auction/collect.py:347) returns False, the check is skipped entirely, and unit_price 1e-09 returns HTTP 201 as a rankable bid. T-224 is HIGH: list_price 0.0 is an accepted roster value (Field(ge=0.0)), giving an unauthenticated HTTP 500 out of the middle of an auction plus a 0.00 rankable fallback on the same row. The other five are T-221 (the k-anonymity floor T-138 delivered defaults to DEFAULT_K_ANONYMITY=1 and build_profile never calls anonymise_cohort — correct code behind a switch that is off), T-222 (a defect converted into documentation and pinned by a passing test, so the suite certifies the broken behaviour), T-225 (a test that asserts nothing and reports green, in three copies: 'for relative in []:'), T-226 (every member tests/ directory is outside the type gate because root pyproject.toml points mypy at .pkgroot, which reaches src/ only), and T-227 (the frozen test and the product can select DIFFERENT manifest documents). THIS IS NOT THE SAME DEFECT AS ESC-011 — those ten tickets have real gates that are spelled wrong; these seven have no gate at all, by design, and are waiting on someone to write one. It is the same PERMISSION, though: both are tickets.json verify fields, and one 'freeze --amend' can carry both.
- **proposed**: Grant this in the SAME amendment 10 as ESC-011 — one amend, two escalations resolved, tickets.json verify fields only. I am NOT asking you to invent seven gates. I am asking for permission to record, as each ticket's verify, a command selecting a reproduction test that DOES NOT EXIST YET and that its lane must write as its first commit — which is exactly the fix-ticket contract ('Reproduce FIRST') and exactly what amendment 8 did for the greenfield tickets by pointing their gates at tests that had to be created. Concretely, the shape for each is 'uv run python -m pytest <the test file the lane will add> -q -k <the reproduction>', e.g. T-223: 'uv run python -m pytest apps/exchange/tests/test_price_floor_proportional.py -q'; T-224: 'uv run python -m pytest apps/exchange/tests/test_roster_zero_list_price.py -q'; T-221: 'uv run python -m pytest apps/buyer/svc/tests/test_k_anonymity_floor.py -q'; T-222: 'uv run python -m pytest apps/exchange/tests/test_accept_offer_durability.py -q'; T-225: 'uv run python -m pytest apps/seller-reference/tests/test_scaffold_smoke.py -q'; T-226: 'uv run python -m mypy'; T-227: 'uv run python -m pytest apps/seller-reference/tests/test_persona_manifest_selection.py -q'. Until the lane writes the file these read MISSING rather than red, so I will NOT treat any of them as a satisfied gate at dispatch — the gate is enforced at COLLECTION, where red-check --close runs against the branch's merge parent and the reproduction test exists by then. If you would rather I write all seven reproduction tests into the repo FIRST, prove each red, and come back with gates that are already failing at base, say so and I will do that instead — it is slower by one round trip and strictly more auditable, and I am happy to take that route. ALTERNATIVE WORTH YOUR RULING: if you would prefer 'findings' minted tickets never need an amendment to become dispatchable, that is a change to the swarm-loop harness rather than to this run, and I would file it upstream instead. NOT BLOCKING — nothing in the current wave depends on this; it decides what cycle 16 can dispatch.
- **decision**: _NONE — NEVER ANSWERED. The loop withdrew the question_: Superseded by ESC-013. ESC-012 said seven finding-tickets carry the placeholder gate; the measured number is 49 of 72 READY tickets, so its scope was wrong by a factor of seven and its proposal (write seven speculative gate strings) was the wrong shape for a problem that size. The underlying cause is a harness defect, now filed at ~/claude-build/observations/swarm-loop-HARNESS-OPEN.md as B09032015-01: swarmloop.py findings should accept a verify on the finding record and seal it in the mint it already re-seals, so an unattended audit loop can dispatch its own findings without a human amendment. ESC-013 asks only for the ten gates the next two cycles need. T-223 (CRITICAL) and T-224 (HIGH) still need gates before they can dispatch and are NOT covered by ESC-013 — I will bring those back with reproduction tests already written and proven red, which is the more auditable route ESC-012 offered as its alternative.

### ESC-010 · frozen-goal · `tickets.json — the 'verify' field on the seven greenfield tickets T-032/T-034/T-035/T-042/T-043/T-044/T-045` — resolved 2026-09-03T15:20:41

- filed 2026-09-03T14:10:31 (cycle 14)
- **what was wrong**: EVERY GREENFIELD TICKET IN THIS WAVE HAS A GATE THAT CANNOT EXIST BEFORE THE WORK, SO NONE OF THEM CAN PASS THE MERGE GATE. This is not hypothetical — T-044 is finished and is being refused right now. Its lane took the frozen E4 acceptance count from 5 to 13 (measured with the frozen runner, verified by eleven independent sabotages of the core that each turn a test red, two of which drop the frozen count 13->12 and so bind the number to that lane's own code). check-branch vetoes the merge anyway, correctly by its own rule: T-044's declared verify is 'pytest packages/store-agent/tests/test_external_bids.py -q', a file the TICKET ITSELF must create, so at the merge base pytest exits 4 with nothing selected and red-check stamps WEAK — 'the gate proved nothing about this ticket'. The stamp is a fact about the command, not about the branch, so re-running red-check re-stamps WEAK forever; check-branch says so itself and tells you to fix the cause in the ticket graph. All seven tickets in this wave have exactly this shape (each verify names a test file the lane must write), so all seven will hit the same veto as they finish. THE VETO IS RIGHT AND I DO NOT WANT IT WEAKENED — a gate that selects nothing is exactly the vacuous-gate failure this repo has been bitten by three times. What is wrong is the COMMAND, and the repair makes the gate stronger rather than weaker. Note the constraint that makes this non-obvious: raw pytest cannot run the frozen suite at all (the root conftest.py hard-aborts on unset PROXYSHOP_WORKER), and the frozen runner run.py has no failing exit mode — it prints numbers and exits 0 even with failures — so neither of the two obvious commands works.
- **proposed**: freeze --amend tickets.json to replace the 'verify' field on those seven tickets with a command that selects the ticket's OWN frozen acceptance tests and fails on them, using the same --confcutdir escape the frozen runner itself uses. Shape, TESTED END TO END on T-044 before proposing it: 'pytest .swarm-loop/acceptance/<the epic file> -q -p no:cacheprovider --confcutdir=.swarm-loop/acceptance -k "<that ticket's test names>"'. Measured for T-044: on main it selects exactly 8 tests and all 8 FAIL (exit non-zero, nothing deselected into vacuity); on task/C14-T044 the same command selects the same 8 and all 8 PASS. That is a gate that discriminates in both directions, which is more than any of the seven have today. Per-ticket mapping: T-032 -> test_e3_exchange.py + test_spec_criteria.py, its 6 ranking tests plus release blockers S8-1/S8-2; T-034 -> test_e3_exchange.py, its 3 exposure tests; T-035 -> test_e3_exchange.py, its 1 loss-report test; T-042 -> test_e4_store_agent.py, its 3 learning tests; T-043 -> test_e4_store_agent.py, its 3 mode tests; T-044 -> test_e4_store_agent.py, its 8 door tests; T-045 -> test_e4_store_agent.py, its 1 persona test. I will verify each is red-at-base with the right number of tests SELECTED before applying it, and report any that is not. This changes no metric, no target and no acceptance test — only the command each ticket declares as its own gate. If you would rather not amend, the alternative is that I merge nothing from this wave and the seven lanes' work sits on branches; I am not going to route around the veto.
- **decision**: APPROVED by Hank in the cycle-14 turn ('I approve'). Landed as amendment 8 at 27ace6f; tickets.json verify-fields only, no metric, target, acceptance test, product file or dependency touched. Every one of the seven new gates was measured RED AT BASE with the exact expected number of tests SELECTED before it was applied (T-032 8 failed/21 deselected, T-034 3, T-035 1, T-042 3, T-043 3, T-044 8, T-045 1), and T-044 was additionally verified GREEN on its finished branch with the same 8 selected — so that gate discriminates in both directions, which none of the old ones did in either. One caution recorded rather than buried: my first verification wrapper reported T-032 as 'no tests ran', which was a bug in my own shell function and not in the gate; run directly the same expression selects 8 and fails 8. What this released: the retro red-check that check-branch demands is now runnable at all, and six finished branches carrying 9917 lines, 27 acceptance tests and both S8 release blockers become mergeable.

### ESC-009 · frozen-goal · `tickets.json — scope globs and depends_on edges on T-032/T-034/T-035 and T-042/T-043/T-044/T-045` — resolved 2026-09-03T13:56:12

- filed 2026-09-03T13:29:55 (cycle 13)
- **what was wrong**: THE GRAPH IS SERIALIZING SIX INDEPENDENT BUILDS, AND THAT IS WHY THE TWO LARGEST METRICS HAVE NEVER MOVED. Measured this cycle, all of it against HEAD 7279452. (1) The 44 red acceptance tests are dominated by E4 (5/20) and E3 (9/21), 27 of the 44. Independent diagnosis of both epics found that EVERY one of those 27 failures is an ImportError or ModuleNotFoundError — not one assertion is reached — because seven whole packages were never written: apps/exchange/src/{ranking,policy,reports}/ and packages/store-agent/src/{learning,modes,external}/ are 0-byte __init__.py files with no siblings, and apps/seller-reference/src/ holds only __init__.py. The tickets that own them (T-032, T-034, T-035, T-042, T-043, T-044, T-045) are all open with every dependency closed, and dispatch.jsonl has ZERO records naming T-042, T-043, T-044 or T-045 across all 114 entries. They were never dispatched in thirteen cycles. (2) The reason is mechanical, not a judgement call. Each of T-032/T-034/T-035 declares scope 'apps/exchange/tests/**' and each of T-042/T-043/T-044 declares 'packages/store-agent/tests/**', so check-wave correctly vetoes every pair of them on a glob intersection - T-032<->T-034, T-032<->T-035, T-034<->T-035, T-042<->T-043, T-042<->T-044, T-043<->T-044. Their REAL ownership is disjoint: test_ranking.py / test_bandit.py / test_loss_reports.py, and test_external_bids.py / test_learning.py / test_modes.py, and each tests directory's conftest.py auto-loads uniquely-named _fixtures_<topic>.py siblings so no lane need edit a shared file. The glob is coarser than the truth, and the veto that follows from it is what keeps only one of each trio in flight. (3) Three depends_on edges are contradicted by the frozen tests themselves: T-034->T-032 and T-035->T-032 (both read in full; the policy and reports tests import nothing from ranking and feed plain fixture dicts) and T-045->T-044 (the persona test imports only the personas module and the manifest - no signing, no door, no queue). I have dispatched the largest legal wave the current globs permit - T-032, T-044, T-045, check-wave OK - so the run is moving regardless of how you rule. What is blocked is the OTHER four, which under the current graph need three more serialized waves to do work that has no real ordering between it.
- **proposed**: freeze --amend tickets.json to do two things and nothing else. (a) Narrow the scope of the six tickets from the directory glob to their actual per-file ownership: T-032 -> apps/exchange/src/ranking/** + apps/exchange/tests/test_ranking.py + apps/exchange/tests/_fixtures_ranking.py; T-034 -> apps/exchange/src/policy/** + test_bandit.py + _fixtures_bandit.py; T-035 -> apps/exchange/src/reports/** + test_loss_reports.py + _fixtures_loss_reports.py; T-042 -> packages/store-agent/src/learning/** + test_learning.py + _fixtures_learning.py; T-043 -> packages/store-agent/src/modes/** + test_modes.py + _fixtures_modes.py; T-044 -> packages/store-agent/src/external/** + test_external_bids.py + _fixtures_external.py. This WEAKENS NOTHING: it makes check-wave's veto sharper, not looser - two lanes that would genuinely write the same file are still refused, and no lane gains the right to touch a file it could not touch before. (b) Drop the three paper edges T-034->T-032, T-035->T-032, T-045->T-044, each of which I verified against the frozen test body rather than the ticket prose. If you would rather not touch depends_on, (a) alone is worth having: it takes the store-agent trio from three waves to one. If you decline both, say so and I will serialize them - it costs cycles, not correctness.
- **decision**: APPROVED by Hank — 'approve ESC-009, do both (a) and (b)'. Landed as amendment 7 (freeze-log record #8); tickets.json only, no metric, target or acceptance test touched, graph validated acyclic with no dangling deps. Verified it did the job: check-wave on all seven of T-032/T-034/T-035/T-042/T-043/T-044/T-045 now exits OK where every pair among the two trios was previously vetoed. What it released, immediately: all seven are dispatched and building in isolated worktrees, the four that were blocked (T-034, T-035, T-042, T-043) among them. If they all land, E3 goes 9->21 and E4 5->20 — 27 of the 44 red acceptance tests.

### ESC-008 · harness · `scripts/verify.sh` — resolved 2026-09-03T13:29:27

- filed 2026-09-03T10:44:33 (cycle 13)
- **what was wrong**: scripts/verify.sh:179-186 is FROZEN and now describes a bug that no longer exists, in the present tense. It says `check` 'now builds the session-scoped _neo4j_guard and holds the MACHINE-GLOBAL flock ... for most of its run (measured: 196s of a 222s session)' and that 'the fix belongs in the fixture's scope, not here'. That fix landed at e478ff4 (T-214, merged cycle 13): _neo4j_guard is function-scoped and the hold is 10.1-10.9% of a run, independently re-measured by an adversarial verifier with its own 20Hz probe. The comment is now false. This is the exact premise-failure class this project keeps paying for: the next agent reads verify.sh for context, believes the 88% hold is live, and either re-opens T-214 or measures against a false baseline. It is PROSE ONLY - no behaviour depends on it - which is why I did not amend it myself: a frozen file is the user's to change, and I will not spend the amendment mechanism on a comment without being asked.
- **proposed**: Either (a) freeze --amend scripts/verify.sh to correct the stale paragraph to past tense naming e478ff4, bundled with any other amendment you approve this cycle so the amendment count does not grow for a comment; or (b) decide the stale comment is acceptable and leave it, in which case this escalation stands as the durable record that it is known-false. I recommend (a) only if you are already amending; otherwise (b) plus this record is cheaper and equally honest.
- **decision**: DECLINED by Hank, agreeing with my recommendation in the cycle-13 turn: do not spend a harness amendment on a comment. The stale paragraph at scripts/verify.sh:179-186 STAYS, and is hereby known-stale — it describes in the present tense a machine-global flock behaviour that T-214 (merged e478ff4) removed, and the fix it prescribes ('the fix belongs in the fixture') has already been applied. Recorded here so a future reader does not act on it. Standing instruction for later: if scripts/verify.sh is ever amended for a SUBSTANTIVE reason, correct this paragraph to past tense naming e478ff4 in the same amendment, so the comment costs no amendment of its own.

### ESC-007 · frozen-test · `.swarm-loop/acceptance/test_e4_store_agent.py::_claims_in / _claim_identity` — resolved 2026-09-03T11:14:09

- filed 2026-09-02T23:30:29 (cycle 10)
- **what was wrong**: A FROZEN HELPER WILL FAIL ON CORRECT CODE, and it is latent only by fixture accident. T-135 (landed this cycle) tightened contracts.boundary to REQUIRE provenance on offer.discount — an unprovenanced discount is now refused, correctly, because otherwise an attacker drops the provenance block instead of relabelling it. But the frozen E4 helper _claims_in treats EVERY provenance-bearing node as a claim and _claim_identity keys it on (key, value, source, ref). A Discount has no 'key'. So the very provenance T-135 now requires makes the discount node unmatchable, and it reads as smuggled. Measured on the merged tree by the T-041 lane: frozen _claims_in on a DISCOUNTED bid -> smuggled [('None','15.0','envelope_rule','envelope:store-alpha:v3#max_discount_pct@prod-cap')]; on a COLD bid -> smuggled []. test_every_claim_in_a_hosted_bid_traces_to_a_hook_call passes TODAY only because its fixture context is cold (learned_policy=None, no intro rule) so no discount is produced. It will fail on correct code the moment a discounted hosted bid reaches that helper — which is exactly what T-042 and T-043 are scheduled to produce (learned-policy discounts). Neither the T-041 lane nor I touched the frozen suite; the lane pinned the property it does control by adding a cold-path test instead.
- **proposed**: Amend _claim_identity in .swarm-loop/acceptance/test_e4_store_agent.py to identify a provenance-bearing node by a key that exists on every such node — e.g. fall back to the node's path or type when 'key' is absent, so a Discount is identified as (type/path, value, source, ref) rather than ('None', ...). This weakens no assertion and makes nothing easier to pass: it lets the helper MATCH a node it currently cannot see, so a genuinely smuggled discount still fails. Alternative if you prefer the suite untouched: rule that discounted hosted bids are out of scope for that helper and record it, but then T-042/T-043 must be told before they build. Requires freeze --amend because the acceptance suite is hash-frozen.
- **decision**: Granted and landed as amendment 6 at 4f5c592. Applied in the primary tree (check-branch correctly vetoed the lane's branch for touching the frozen harness). Verified weakens-nothing in the primary tree: per-test results identical (15 failed / 5 passed, same 5 passing), the new guard is load-bearing (disabling it turns the hosted-trace test RED), and goals.json metrics plus manifest targets are byte-identical across the amend. The first draft was refused because an adversarial verifier measured it admitting a 100%-off discount wearing a scraped list_price provenance; the repaired version asserts that piggyback class permanently.

### ESC-006 · other · `fixtures/manifest.json:213-246 expected_trust_trajectory (T-080 approved document)` — resolved 2026-09-03T02:47:01

- filed 2026-09-02T22:13:36 (cycle 10)
- **what was wrong**: The approved manifest contradicts ITSELF, and I may not fix it. Replaying all seven dishonest_store.behaviours once per episode against the manifest's own published observation_weights gives 0.234 at ep2 (stated band [0.34,0.50]), 0.154 at ep4 (band [0.26,0.40]), 0.084 at ep9 (band [0.14,0.24]), 0.066 at ep12 (band [0.08,0.18]) — EVERY non-zero episode falls outside its own stated band, and the store crosses the 0.35 threshold at episode 1 rather than the intended ~4-6. Only episode 0 (the prior, 0.5) is inside. Measured by the T-062 lane while building the scoring engine that reads those same weights. Latent today: test_e8_proofs.py:413 checks the trajectory's SHAPE, not the engine's numbers, so nothing asserts it. It stops being latent the moment T-081's simulator is graded against it — that ticket would fail through no fault of its own, and the failure would look like a simulator bug rather than a document inconsistency. Either the simulator must not replay every behaviour every episode, or the two halves of the approved document disagree.
- **proposed**: A human decision, and deliberately NOT one I will make: fixtures/manifest.json is T-080 single-writer ground truth, its approval digest covers the body, and it is pending_human_approval. Please decide which half is authoritative — (a) recompute expected_trust_trajectory from the published observation_weights so the document is self-consistent, or (b) keep the trajectory and state the replay schedule it assumes (i.e. that the simulator does NOT replay every behaviour every episode), so T-081 can be built against a rule rather than inferring one. Then re-run the approval yourself: python -m fixtures.approval --approver "...". I have not generated or modified the approval and will not.
- **decision**: RESOLVED. Trajectory recomputed from the manifest's own published weights with the shipped engine (0.5 / 0.235 / 0.158 / 0.120 / 0.090 / 0.073), derived twice independently — before and after cycle 11 changed scoring — identical both times. The replay schedule those numbers assume is now recorded as part of the contract, since it was previously only inferable, which is how the two halves drifted apart. Hank Holcomb approved the result himself at 2026-09-03T07:14:09Z against body digest 0ce80606; no agent filled approver/approved_at/artifact. Recorded consequence: under the published weights the store crosses the 0.35 threshold at episode 1, not 4-6; the lever for a slower demo decline is the replay schedule, not these numbers. The approval also exposed five guard tests that only passed while the manifest was unapproved — fixed on the same merge, proven wrong by counterfactual execution rather than argument.

### ESC-005 · harness · `scripts/verify.sh:105 (T-117)` — resolved 2026-09-03T01:28:03

- filed 2026-09-02T21:34:28 (cycle 10)
- **what was wrong**: T-117 is CONFIRMED real but structurally undispatchable, so it will sit open forever unless you rule. The defect: a lane can report './scripts/verify.sh check' GREEN while the very tests written to grade its acceptance were silently DESELECTED — the gate cannot distinguish 'passed' from 'never ran'. Cycle 9 already lost integration time twice to gate blindness of this class, and red-check this cycle found two ticket gates (T-111, T-123) that EXIT 0 with their defect live, which is the same failure wearing a different hat. Why a worker cannot fix it: the fix belongs at scripts/verify.sh:105, a protected path — check-branch vetoes any branch touching it, correctly and every time. The ticket's own non_goals forbid dodging that veto by relocating the logic into an unprotected file, and its fallback half (conftest.py + proxyshop_support/**) collides head-on with the in-flight bootstrap lane (T-109/T-111/T-123). Verified by the cycle-10 triage pass against HEAD.
- **proposed**: Amend scripts/verify.sh to make DESELECTION visible and fatal where it matters: after each run_pytest, assert the deselected count is zero for the gate's own selection (or emit PASSED/SKIPPED/DESELECTED counts and fail when a gate selected zero of its named tests), mirroring the existing exit-5 'collected 0 tests' rule that already treats an empty suite as failure. This weakens no assertion and makes nothing easier to pass — it only stops a vacuous green being indistinguishable from a real one. Requires 'freeze --amend' because verify.sh is hash-frozen; not something the orchestrator or any worker may do.
- **decision**: AMENDED AND LANDED. Hank authorized directly in-session. verify.sh run_pytest now emits a SELECTION line naming deselected/skipped counts (acceptance 2), and 'check' no longer blanket-deselects docker (acceptance 1 and 3) now that T-109 makes docker tests skip per-service. Measured effect: check selects 3908 tests where it selected ~3520 — ~388 docker tests every per-ticket gate had been silently dropping, including the ones grading their own tickets. Verified both directions before amending; freeze --amend recorded in freeze-log.jsonl.

### ESC-004 · frozen-test · `test_spec_criteria.py::test_migrations_never_grant_exchange_access_to_sealed_or_vault` — resolved 2026-09-01T15:29:47

- filed 2026-09-01T15:22:21 (cycle 1)
- **what was wrong**: Reported by T-011 and confirmed by the orchestrator reading the frozen bytes. The test's per-statement filter is 'if "grant" not in low or not _EXCHANGE_ROLE_RE.search(low): continue', where _EXCHANGE_ROLE_RE is r'\bexchange[a-z0-9_]*\b'. A statement such as 'GRANT USAGE ON SCHEMA sealed TO PUBLIC; GRANT SELECT ON ALL TABLES IN SCHEMA sealed TO PUBLIC;' contains 'grant' but does NOT contain the token 'exchange', so it is skipped before any inspection. The schema-union regex that builds granted_schemas sits inside that same loop, so it never sees the statement either. Net effect: PUBLIC includes every role, so the exchange role inherits read access to sealed.envelopes while the frozen assertion granted_schemas == {'ledger','app'} still passes with zero violations. S7 is an explicit release blocker ('the exchange cannot read envelopes') and this is the static half of its proof. T-011 verified the bypass live against a real database before reporting it. MITIGATING FACTS, which is why I am not recommending an amendment: (1) db/migrations/** is T-011's EXCLUSIVE scope and no other ticket in the 47-ticket graph can add a migration, so the blast radius is one ticket that has already shipped; (2) T-011's own 0004 migration now revokes object-level privileges from PUBLIC, so a correct re-run repairs a breached database; (3) T-011 shipped test_no_object_in_the_four_schemas_is_granted_to_public, which asserts the property LIVE against a real cluster - strictly stronger evidence than the frozen static scan could ever provide, since it observes actual ACLs rather than parsing SQL text.
- **proposed**: RECOMMEND NO AMENDMENT. The guarantee is now covered by a live test in T-011's own suite that is stronger than the frozen static scan, and no other ticket can write migrations. Record the frozen test's limitation so no future reader mistakes its green for a complete S7 proof, and require any re-dispatch of T-011 (or any audit-loop ticket that touches db/migrations) to keep the live PUBLIC assertion. If you would rather close it in the harness, the amendment is small - drop the _EXCHANGE_ROLE_RE precondition and inspect every GRANT statement, treating a PUBLIC grantee on sealed/vault as a violation - but it costs amendment 3 plus an immediate cycle-0 re-measure for a hole nothing can currently drive through.
- **decision**: Hank agreed with the recommendation: NO amendment. The frozen suite stays at amendment 2. The frozen S7 grant test's blind spot is real and confirmed by reading the bytes — it skips any statement not containing the token 'exchange', so a GRANT ... TO PUBLIC on sealed/vault is never inspected and granted_schemas still reads {ledger, app}. It is not being closed in the harness because: (1) db/migrations/** is T-011's exclusive scope and no other ticket in the 47-ticket graph can add a migration, bounding the exposure to one already-shipped ticket; (2) T-011's 0004 migration revokes object-level privileges from PUBLIC, so a correct re-run repairs a breached database; (3) T-011 shipped test_no_object_in_the_four_schemas_is_granted_to_public, which observes real ACLs on a live cluster — strictly stronger evidence than any static SQL text scan, since it sees actual grants rather than parsing statements. The frozen test's green is therefore evidence about the parse, not about the database, and that limitation is recorded in learnings.md so no later reader mistakes it for a complete S7 proof. Standing obligation: any re-dispatch of T-011, and any audit-loop ticket touching db/migrations, must keep the live PUBLIC assertion.

### ESC-003 · frozen-test · `test_e6_trust.py::test_verification_results_are_typed_and_route_into_the_catalog_claim_dimension` — resolved 2026-09-01T14:53:51

- filed 2026-09-01T14:52:38 (cycle 1)
- **what was wrong**: The test is marked ticket T-065 but imports 'from apps.trust.src.scoring import claim_dimension, score' (test_e6_trust.py:1470). apps/trust/src/scoring/** is T-062's exclusive scope. T-062.depends_on = [T-060, T-065, T-080] - it depends on T-065 - while T-065.depends_on = [T-010, T-021, T-060, T-080] has no T-062 edge. So T-065 schedules and is graded BEFORE T-062, and at that moment the module it imports does not exist; the import sits inside the test function, so it is an ImportError at call time. One of T-065's six frozen tests therefore cannot pass at T-065's build time, and the obvious repair is unavailable: adding T-065 -> T-062 creates a genuine cycle, which I verified programmatically. I independently AST-swept all 120 tests - extracting each test's ticket marker, its function-level product imports, each imported module's owning ticket by scope glob, and whether that owner lies in the marked ticket's transitive dependency closure. Exactly ONE violation exists in the whole suite and this is it, so the blast radius is one test, not a class. IMPORTANT for the ruling: the frozen METRIC is unaffected either way. e6_trust_passing counts passing tests per epic, not per ticket, and this test is E6 under both markers - so once T-062 lands the test passes and the metric arrives on schedule with no amendment at all. What is actually broken is only T-065's ticket-closure bookkeeping, which is an orchestrator convention rather than a frozen artifact.
- **proposed**: Two viable options, and I recommend the second. (A) Amend the frozen suite to re-marker this test from T-065 to T-062 - defensible on the merits, since the docstring's assertion is about routing a typed claim INTO the dimension, which is scoring's job, and T-062 already owns the other ten catalog_claim_accuracy tests; because T-062 depends on T-065 both modules exist at T-062's build time, so it resolves with no new edge. Costs amendment 3 and a re-measure. (B) No amendment: record that T-065's sixth frozen test is graded only after T-062 lands, put that in T-065's packet so its worker does not thrash on an ImportError it cannot fix from inside its own scope, and close T-065 on its other five plus its own verify command. The metric is identical under both, so A buys ledger tidiness and B buys one fewer amendment.
- **decision**: Hank ruled: NO amendment. Option B. The frozen suite is untouched and stays at amendment 2. Rationale: the frozen metric is indifferent — e6_trust_passing counts passing tests per EPIC, and this test is E6 under either marker, so it goes green when T-062 lands whichever ticket is blamed for it. The only thing at stake was T-065's ticket-closure bookkeeping, which is an orchestrator convention, not a frozen artifact, so spending amendment 3 and a re-measure on it would have bought nothing the metric can see. Actions taken instead: (1) T-065's packet will state that its sixth frozen test, test_verification_results_are_typed_and_route_into_the_catalog_claim_dimension, is graded only after T-062 lands and must NOT be chased from inside T-065's scope — the ImportError is expected and unfixable there; (2) T-065 closes on its other five frozen tests plus its own verify command; (3) recorded as a standing scheduling constraint in backlog.md so it survives this session. Verified independently before ruling: an AST sweep of all 120 tests for tests whose marked ticket's transitive dependency closure excludes the owner of a module they import found exactly ONE violation, this one, and confirmed that adding T-065 -> T-062 would create a cycle.

### ESC-002 · frozen-test · `test_spec_criteria.py::test_compose_declares_healthchecked_postgres_neo4j_redis_and_separate_deployables is unsatisfiable` — resolved 2026-09-01T14:48:11

- filed 2026-09-01T14:07:08 (cycle 1)
- **what was wrong**: The test (test_spec_criteria.py:741-751) asserts a compose service whose name contains buyer/merchant/exchange/trust exists. It reads services via _compose_services() (:280), a deliberately TEXTUAL parser of the top-level 'services:' block of the single file _compose_path() (:267) returns - the root docker-compose.yml. It never follows 'include:'. Verified against the tree: the root file's services: block declares exactly postgres, neo4j and redis; the four deployables live in apps/{buyer,exchange,trust,merchant}/compose.yaml, pulled in by the include: at docker-compose.yml:11-20, and all four fragments exist on disk. The root file's own header states the contract the test contradicts: 'the nine include: fragments below are pre-created empty stubs inside their owners' scope globs, so a service ticket fills its own fragment and no ticket ever edits this file.' So every service ticket can fill its fragment exactly as designed, 'docker compose config' can resolve all four, and this test stays red for the entire run. Because acceptance_pass_rate is target 100 with tolerance 0, this single test pins the run's top-line metric below target permanently and holds spec_criteria_passing at 7/8. Note docker-compose.yml is NOT among the 16 hashed files, so duplicating the four services into the root would cost no frozen byte - but it contradicts the file's stated design and would collide with the include: fragments on duplicate service names.
- **proposed**: Preferred: amend the frozen test so _compose_services() resolves the include: fragments (or so the deployable assertion reads the fragments directly), matching the architecture the scaffold actually shipped. Alternative if you would rather not amend: rule that the four deployables are declared in the root docker-compose.yml after all, and retire the 'no ticket ever edits this file' clause in its header - but that collides with the existing include: entries and I do not recommend it. Third option: accept acceptance_pass_rate topping out below 100 and record it as a known, explained red for the run.
- **decision**: Hank approved amending the frozen test. Applied as amendment 2 and re-frozen: two new helpers in test_spec_criteria.py, _compose_include_paths() (textual include: parser, no PyYAML, consistent with _compose_services) and _compose_stack_services() (unions root services with every included fragment's), and the test now reads that union. Verified in BOTH directions before re-freezing: with the four deployable fragments as shipped (empty 'services: {}' stubs) the test is RED for the correct reason, and with the four temporarily filled it is GREEN — so C1 became reachable without becoming free. No assertion weakened, none renamed, none added or removed; suite stays at 120. Cycle 0 re-measured immediately after the amend (analyze reads the stored error column, so a stale row would fit against a dead target): reading is unchanged at 3.33 / 4 spec criteria / 120 collected, confirming no free point was granted.

### ESC-001 · blocked-ticket · `T-080 scope cannot create fixtures/manifest.json, fixtures/generator, fixtures/__init__.py` — resolved 2026-09-01T14:40:35

- filed 2026-09-01T14:06:49 (cycle 1)
- **what was wrong**: T-080's scope is seven DIRECTORY globs: fixtures/{manifest,approval,seed,catalog,personas,golden,tests}/**. The frozen suite pins the manifest as a FILE at the fixtures root: test_e8_proofs.py:69 MANIFEST_REL = 'fixtures/manifest.json', re-read at test_e6_trust.py:294, and test_e8_proofs.py:526 does 'from fixtures.generator import apply, generate'. I ran the glob-ownership derivation across all 47 tickets: fixtures/manifest.json, fixtures/generator.py and fixtures/__init__.py match ONLY T-000's universal '**' glob, and T-000 is closed. fixtures/manifest/** matches paths INSIDE the empty scaffold directory, not the file. Worse than merely missing: test_e8_proofs.py:305-313 asserts no stray manifest.json exists anywhere under fixtures/ except the canonical path, so T-080 writing fixtures/manifest/manifest.json (its only in-scope option) trips that assertion and fails loudly. Blast radius: all 8 e8_proofs tests (target 8, currently 0) plus the 6 e6_trust tests that route through _manifest() (6 of target 26). T-080 is also the human-approval gate blocking 16 tickets, so this fails at the worst point in the graph. tickets.json is NOT in the 16-file frozen manifest, so the fix costs no frozen byte and needs no amendment - but ticket ownership is a packet boundary, which is the user's to change.
- **proposed**: Widen T-080.scope by three explicit entries: 'fixtures/manifest.json', 'fixtures/generator/**' (or 'fixtures/generator*' if it is a single module), and 'fixtures/__init__.py'. Keep the seven existing directory globs. Verified disjoint from every other ticket's scope, so parallel_safe stays truthful and the retired fixtures/** overlap hazard does not return.
- **decision**: Hank approved widening T-080's scope. Applied to tickets.json: added 'fixtures/manifest.json', 'fixtures/generator/**', 'fixtures/__init__.py' to the seven existing directory globs. Re-derived ownership across all 47 tickets: T-080 is now the sole non-T-000 owner of all three paths, and no other open ticket declares any scope under fixtures/ except its own subdirectory, so parallel_safe stays truthful and the retired fixtures/** overlap hazard does not return. tickets.json is not a hashed file; no frozen byte moved and no amendment was needed.
