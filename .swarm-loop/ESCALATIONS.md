# ESCALATIONS — proxyshop

The loop writes here when it hits something frozen (or otherwise locked) that it
believes is **wrong**. A freeze is a locked door, not a wall: the loop's obligation is
to bring the problem to you rather than work around it, silently re-freeze it, or keep
scoring against a goal it thinks is broken.

**Nothing in this file has been changed by the loop. Escalating is asking. It is never
granting.** Every entry is a request awaiting your decision. Only you can grant one:
`freeze --amend "<reason>"` is the only mechanism that alters a frozen goal or test,
and `escalate --resolve <ID>` records what you decided.

<sub>Mirror of `.swarm-loop/escalations.jsonl` (append-only — that file is the source of truth). Regenerated by
every `escalate`; editing this file by hand changes nothing.</sub>

## OPEN (0)

_None. Nothing is waiting on you._

## RESOLVED (3)

### ESC-003 · frozen-test · `test_e6_trust.py::test_verification_results_are_typed_and_route_into_the_catalog_claim_dimension` — resolved 2026-09-01T14:53:51

- filed 2026-09-01T14:52:38 (cycle 1)
- **what was wrong**: The test is marked ticket T-065 but imports 'from apps.trust.src.scoring import claim_dimension, score' (test_e6_trust.py:1470). apps/trust/src/scoring/** is T-062's exclusive scope. T-062.depends_on = [T-060, T-065, T-080] - it depends on T-065 - while T-065.depends_on = [T-010, T-021, T-060, T-080] has no T-062 edge. So T-065 schedules and is graded BEFORE T-062, and at that moment the module it imports does not exist; the import sits inside the test function, so it is an ImportError at call time. One of T-065's six frozen tests therefore cannot pass at T-065's build time, and the obvious repair is unavailable: adding T-065 -> T-062 creates a genuine cycle, which I verified programmatically. I independently AST-swept all 120 tests - extracting each test's ticket marker, its function-level product imports, each imported module's owning ticket by scope glob, and whether that owner lies in the marked ticket's transitive dependency closure. Exactly ONE violation exists in the whole suite and this is it, so the blast radius is one test, not a class. IMPORTANT for the ruling: the frozen METRIC is unaffected either way. e6_trust_passing counts passing tests per epic, not per ticket, and this test is E6 under both markers - so once T-062 lands the test passes and the metric arrives on schedule with no amendment at all. What is actually broken is only T-065's ticket-closure bookkeeping, which is an orchestrator convention rather than a frozen artifact.
- **proposed**: Two viable options, and I recommend the second. (A) Amend the frozen suite to re-marker this test from T-065 to T-062 - defensible on the merits, since the docstring's assertion is about routing a typed claim INTO the dimension, which is scoring's job, and T-062 already owns the other ten catalog_claim_accuracy tests; because T-062 depends on T-065 both modules exist at T-062's build time, so it resolves with no new edge. Costs amendment 3 and a re-measure. (B) No amendment: record that T-065's sixth frozen test is graded only after T-062 lands, put that in T-065's packet so its worker does not thrash on an ImportError it cannot fix from inside its own scope, and close T-065 on its other five plus its own verify command. The metric is identical under both, so A buys ledger tidiness and B buys one fewer amendment.
- **decision**: Hank ruled: NO amendment. Option B. The frozen suite is untouched and stays at amendment 2. Rationale: the frozen metric is indifferent — e6_trust_passing counts passing tests per EPIC, and this test is E6 under either marker, so it goes green when T-062 lands whichever ticket is blamed for it. The only thing at stake was T-065's ticket-closure bookkeeping, which is an orchestrator convention, not a frozen artifact, so spending amendment 3 and a re-measure on it would have bought nothing the metric can see. Actions taken instead: (1) T-065's packet will state that its sixth frozen test, test_verification_results_are_typed_and_route_into_the_catalog_claim_dimension, is graded only after T-062 lands and must NOT be chased from inside T-065's scope — the ImportError is expected and unfixable there; (2) T-065 closes on its other five frozen tests plus its own verify command; (3) recorded as a standing scheduling constraint in backlog.md so it survives this session. Verified independently before ruling: an AST sweep of all 120 tests for tests whose marked ticket's transitive dependency closure excludes the owner of a module they import found exactly ONE violation, this one, and confirmed that adding T-065 -> T-062 would create a cycle.

### ESC-002 · frozen-test · `test_spec_criteria.py::test_compose_declares_healthchecked_postgres_neo4j_redis_and_separate_deployables is unsatisfiable` — resolved 2026-09-01T14:48:11

- filed 2026-09-01T14:07:08 (cycle 1)
- **what was wrong**: The test (test_spec_criteria.py:741-751) asserts a compose service whose name contains buyer/merchant/exchange/trust exists. It reads services via _compose_services() (:280), a deliberately TEXTUAL parser of the top-level 'services:' block of the single file _compose_path() (:267) returns - the root docker-compose.yml. It never follows 'include:'. Verified against the tree: the root file's services: block declares exactly postgres, neo4j and redis; the four deployables live in apps/{buyer,exchange,trust,merchant}/compose.yaml, pulled in by the include: at docker-compose.yml:11-20, and all four fragments exist on disk. The root file's own header states the contract the test contradicts: 'the nine include: fragments below are pre-created empty stubs inside their owners' scope globs, so a service ticket fills its own fragment and no ticket ever edits this file.' So every service ticket can fill its fragment exactly as designed, 'docker compose config' can resolve all four, and this test stays red for the entire run. Because acceptance_pass_rate is target 100 with tolerance 0, this single test pins the run's top-line metric below target permanently and holds spec_criteria_passing at 7/8. Note docker-compose.yml is NOT among the 16 hashed files, so duplicating the four services into the root would cost no frozen byte - but it contradicts the file's stated design and would collide with the include: fragments on duplicate service names.
- **proposed**: Preferred: amend the frozen test so _compose_services() resolves the include: fragments (or so the deployable assertion reads the fragments directly), matching the architecture the scaffold actually shipped. Alternative if you would rather not amend: rule that the four deployables are declared in the root docker-compose.yml after all, and retire the 'no ticket ever edits this file' clause in its header - but that collides with the existing include: entries and I do not recommend it. Third option: accept acceptance_pass_rate topping out below 100 and record it as a known, explained red for the run.
- **decision**: Hank approved amending the frozen test. Applied as amendment 2 and re-frozen: two new helpers in test_spec_criteria.py, _compose_include_paths() (textual include: parser, no PyYAML, consistent with _compose_services) and _compose_stack_services() (unions root services with every included fragment's), and the test now reads that union. Verified in BOTH directions before re-freezing: with the four deployable fragments as shipped (empty 'services: {}' stubs) the test is RED for the correct reason, and with the four temporarily filled it is GREEN — so C1 became reachable without becoming free. No assertion weakened, none renamed, none added or removed; suite stays at 120. Cycle 0 re-measured immediately after the amend (analyze reads the stored error column, so a stale row would fit against a dead target): reading is unchanged at 3.33 / 4 spec criteria / 120 collected, confirming no free point was granted.

### ESC-001 · blocked-ticket · `T-080 scope cannot create fixtures/manifest.json, fixtures/generator, fixtures/__init__.py` — resolved 2026-09-01T14:40:35

- filed 2026-09-01T14:06:49 (cycle 1)
- **what was wrong**: T-080's scope is seven DIRECTORY globs: fixtures/{manifest,approval,seed,catalog,personas,golden,tests}/**. The frozen suite pins the manifest as a FILE at the fixtures root: test_e8_proofs.py:69 MANIFEST_REL = 'fixtures/manifest.json', re-read at test_e6_trust.py:294, and test_e8_proofs.py:526 does 'from fixtures.generator import apply, generate'. I ran the glob-ownership derivation across all 47 tickets: fixtures/manifest.json, fixtures/generator.py and fixtures/__init__.py match ONLY T-000's universal '**' glob, and T-000 is closed. fixtures/manifest/** matches paths INSIDE the empty scaffold directory, not the file. Worse than merely missing: test_e8_proofs.py:305-313 asserts no stray manifest.json exists anywhere under fixtures/ except the canonical path, so T-080 writing fixtures/manifest/manifest.json (its only in-scope option) trips that assertion and fails loudly. Blast radius: all 8 e8_proofs tests (target 8, currently 0) plus the 6 e6_trust tests that route through _manifest() (6 of target 26). T-080 is also the human-approval gate blocking 16 tickets, so this fails at the worst point in the graph. tickets.json is NOT in the 16-file frozen manifest, so the fix costs no frozen byte and needs no amendment - but ticket ownership is a packet boundary, which is the user's to change.
- **proposed**: Widen T-080.scope by three explicit entries: 'fixtures/manifest.json', 'fixtures/generator/**' (or 'fixtures/generator*' if it is a single module), and 'fixtures/__init__.py'. Keep the seven existing directory globs. Verified disjoint from every other ticket's scope, so parallel_safe stays truthful and the retired fixtures/** overlap hazard does not return.
- **decision**: Hank approved widening T-080's scope. Applied to tickets.json: added 'fixtures/manifest.json', 'fixtures/generator/**', 'fixtures/__init__.py' to the seven existing directory globs. Re-derived ownership across all 47 tickets: T-080 is now the sole non-T-000 owner of all three paths, and no other open ticket declares any scope under fixtures/ except its own subdirectory, so parallel_safe stays truthful and the retired fixtures/** overlap hazard does not return. tickets.json is not a hashed file; no frozen byte moved and no amendment was needed.
