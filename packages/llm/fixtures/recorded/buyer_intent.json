{
  "provenance": {
    "source": "hand-authored from the buyer intent model in SPEC.md R19 (hard constraints are eligibility filters field/op/value; preferences are scores field/direction/weight)",
    "doc_version": "Anthropic Messages API, docs.claude.com/en/api/messages, as published 2026-02; BUYER_MODEL per .env.example",
    "authored_by": "T-014 (packages/llm)",
    "authored_at": "2026-02-14",
    "capture": "none - hand-authored under D21; verification is offline (D3)",
    "review": "committed on branch task/T-014 for human review (D21)",
    "notes": "`system` is the intent contract that goes in the system block and is byte-for-byte `buyer_svc.intent.clarifier.INTENT_CONTRACT`; RecordedLLM keys on the (system, prompt) PAIR, so the two move together or every recording silently stops matching. It names a reply format because `parse_llm_reply` reads a JSON object and nothing else, and the old contract never said so: measured against the live model, it answered the cherry-wood dialogue with a correct Markdown table that the parser discarded unread. Each key is the buyer's utterance as the user turn. Replies are JSON strings for RecordedLLM.complete_json(). The constraint/preference split mirrors R19; packages/contracts owns the authoritative Intent schema and T-071 owns the clarifier."
  },
  "system": "INTENT CONTRACT\nSplit the request into hard constraints (field/op/value, filters) and preferences (field/direction/weight, scores). Ask a clarifying question only when a constraint is unusable.\nReply with a single JSON object and nothing else - no prose, no Markdown, no code fence. Its keys are \"hard_constraints\" (a list of {\"field\",\"op\",\"value\"}), \"preferences\" (a list of {\"field\",\"direction\",\"weight\"}) and \"clarifying_question\" (a string, or null when nothing needs asking).\nop is one of \"eq\", \"lte\", \"gte\", \"in\", \"contains\". direction is one of \"maximize\", \"minimize\", \"prefer\". weight is a number between 0 and 1. field is a snake_case attribute name; put a unit in the name rather than in the value, so a width in inches is \"width_in\". Omit a list rather than inventing an entry for it.",
  "recordings": {
    "BUYER\nI want a light roast under $20, and I'd rather it be recently roasted.": "{\"constraints\": [{\"field\": \"roast_level\", \"op\": \"eq\", \"value\": \"light\"}, {\"field\": \"price_usd\", \"op\": \"lt\", \"value\": 20}], \"preferences\": [{\"field\": \"days_since_roast\", \"direction\": \"min\", \"weight\": 1.0}], \"clarifying_question\": null}",
    "BUYER\nsomething good for espresso, cheap": "{\"constraints\": [{\"field\": \"brew_method\", \"op\": \"eq\", \"value\": \"espresso\"}], \"preferences\": [{\"field\": \"price_usd\", \"direction\": \"min\", \"weight\": 1.0}], \"clarifying_question\": \"What is the most you would want to spend per 12 oz bag?\"}"
  }
}
